# Example environment configuration

# Bearer Token required to authenticate API requests to this worker.
# Note: The dispatcher refers to this as SERVERLESS_BEARER_TOKEN
COMFYUI_AUTH_TOKEN=<SERVERLESS_BEARER_TOKEN>

# S3 Upload Configuration
# When set, images will be uploaded to S3 instead of returned as base64
# Note: The dispatcher refers to these as AWS_ACCESS_KEY_ID and AWS_SECRET_ACCESS_KEY
BUCKET_ENDPOINT_URL=https://my-bucket.s3.us-east-1.amazonaws.com
BUCKET_ACCESS_KEY_ID=<your_aws_access_key_id>
BUCKET_SECRET_ACCESS_KEY=<your_aws_secret_access_key>

# Path to Runpod Mounted Volume (if any)
# Optional. The default value of '/runpod-volume' is used in setup_models if this var not provided
NETWORK_VOLUME_PATH=/runpod-volume
